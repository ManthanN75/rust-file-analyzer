# Rust File Analyzer
# 🦀 Rust File Analyzer

A parallel file analyzer that scans `.txt` files in a folder and reports:

- Total lines
- Words
- Characters

Built in Rust using:
- `rayon` for parallelism
- `chrono` for timing
- (optional) CSV export
